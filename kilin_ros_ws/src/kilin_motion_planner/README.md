# Kilin motion planner

This is the runtime planner library used by `kilin_known_terrain_controller`.
It is vendored into the ROS workspace so controller profiles and planner code
are installed together.  The canonical development copy remains under
`planning/src/kilin_motion_planner`; keep this runtime copy synchronized when
planner changes are intentionally adopted by ROS.
